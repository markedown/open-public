# Keep the build context small and reproducible. The stylesheet and binary are
# built inside the image, and provenance comes from build args, not .git.
/target
/.git
/.bin
/.env
/.env.*

# Data fixtures and scratch are never part of the server image.
/fixtures
/scratchpad
