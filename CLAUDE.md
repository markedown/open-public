CLAUDE.md

The project brief for coding agents lives in [`AGENTS.md`](./AGENTS.md), the
tool-agnostic convention. Read it in full; it is the single source of truth for
the stack, the schema, the content rules, and the conventions.

Claude Code reads this file automatically, so this pointer exists to send you
there. Private, machine-local context is kept in a gitignored `CLAUDE.local.md`
next to this file, read alongside it, and never committed.
