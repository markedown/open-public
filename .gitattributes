# The dataset is generated by an export, not written by hand. Marking it keeps
# it out of the repository's language statistics and collapses it by default in
# review, while leaving the diffs themselves readable when they matter.
dataset/*.json linguist-generated=true
